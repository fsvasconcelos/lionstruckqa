import { StorageCache } from "./storage-cache";
export declare class PerformanceLogger {
    _storageCache: StorageCache;
    constructor();
    sampleStart(stepName: string, instanceId: string): void;
    sampleEnd(stepName: string, instanceId: string): void;
    flush(fileName: string, isTestPassed: boolean): void;
    analyzeResults(sourceLogFileName: string, saveResultsFilePath: string, dropResultsFromFailedTest: boolean | undefined): Promise<void>;
    private setSample;
}
